# Setup Guide // Bauhaus Minimalist Profile

## 0. The Repository
This profile README lives in a repository named **exactly** `YashRaut24/YashRaut24` — the special repository GitHub renders on your profile page.

```bash
git init
git add .
git commit -m "feat: minimalist bauhaus constructivist profile"
git branch -M main
git remote add origin https://github.com/YashRaut24/YashRaut24.git
git push -u origin main
```

## 1. Bauhaus Design System Specifications
All SVGs are 100% self-contained native vectors with smooth kinetic SVG animations:
- **Canvas**: Deep Architectural Charcoal (`#111215`)
- **Panels**: `#16171C` with structural grid rules (`#2C303B`)
- **International Klein Blue**: `#2563EB`
- **Bauhaus Crimson Red**: `#E11D48`
- **Bauhaus Warm Ochre**: `#F59E0B`
- **Primary Typography**: Warm Cream (`#FDFBF7`)
- **Kinetic Elements**: Rotating 3D wireframe polyhedron and kinetic metric compass
